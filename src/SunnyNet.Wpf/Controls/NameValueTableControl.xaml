<UserControl x:Class="SunnyNet.Wpf.Controls.NameValueTableControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:SunnyNet.Wpf.Controls">
    <Border Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource DividerBrush}"
            BorderThickness="1"
            CornerRadius="8"
            SnapsToDevicePixels="True">
        <Grid>
            <DataGrid x:Name="RowsGrid"
                      ItemsSource="{Binding Rows, RelativeSource={RelativeSource AncestorType=controls:NameValueTableControl}}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      MouseDoubleClick="RowsGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="名称"
                                        Binding="{Binding Name}"
                                        Width="180"
                                        MinWidth="110"
                                        ElementStyle="{StaticResource DetailNameCellTextStyle}" />
                    <DataGridTextColumn Header="值"
                                        Binding="{Binding Value}"
                                        Width="*"
                                        MinWidth="180"
                                        ElementStyle="{StaticResource DetailValueCellTextStyle}" />
                    <DataGridTextColumn x:Name="ExtraColumn"
                                        Header="其他值"
                                        Binding="{Binding Extra}"
                                        Width="220"
                                        MinWidth="140"
                                        ElementStyle="{StaticResource DetailExtraCellTextStyle}"
                                        Visibility="Collapsed" />
                </DataGrid.Columns>
            </DataGrid>

            <Border x:Name="EmptyPanel"
                    Background="#F8FAFD"
                    IsHitTestVisible="False">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock HorizontalAlignment="Center"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="22"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Text="&#xE71A;" />
                    <TextBlock x:Name="EmptyTextBlock"
                               Margin="0,8,0,0"
                               HorizontalAlignment="Center"
                               Style="{StaticResource CaptionTextStyle}"
                               Text="无内容" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
