<Window x:Class="SunnyNet.Wpf.Windows.SessionNotesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑注释"
        Width="430"
        Height="236"
        MinWidth="380"
        MinHeight="220"
        ResizeMode="CanResize"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        WindowStartupLocation="CenterOwner"
        PreviewKeyDown="SessionNotesWindow_PreviewKeyDown">
    <Border Margin="14" Style="{StaticResource CardBorderStyle}">
        <Grid Margin="16,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Style="{StaticResource DialogTitleTextStyle}" Text="编辑会话注释" />
                <TextBlock x:Name="SubtitleTextBlock"
                           Margin="0,4,0,0"
                           Style="{StaticResource DialogSubtitleTextStyle}"
                           Text="留空保存即可清除注释。" />
            </StackPanel>

            <TextBox x:Name="NotesTextBox"
                     Grid.Row="1"
                     Margin="0,12,0,0"
                     MinHeight="86"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />

            <DockPanel Grid.Row="2"
                       Margin="0,12,0,0"
                       LastChildFill="False">
                <TextBlock VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12"
                           Text="Ctrl+Enter 保存，Esc 关闭" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Margin="0,0,8,0"
                            Padding="12,6"
                            Click="Cancel_Click"
                            Content="取消" />
                    <Button Padding="18,6"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="Save_Click"
                            Content="保存" />
                </StackPanel>
            </DockPanel>
        </Grid>
    </Border>
</Window>
